# ===================================
# AI Stock Analysis System - Dependencies
# ===================================

# Core dependencies
python-dotenv>=1.0.0        # Environment variable configuration management
tenacity>=8.2.0             # Retry mechanism (exponential backoff)
sqlalchemy>=2.0.0           # ORM database operations
schedule>=1.2.0             # Scheduled task dispatcher
exchange-calendars>=4.5.0   # Trading calendars (A-shares/HK/US, Issue #373)

# Data source dependencies (multi-source strategy, ordered by priority)
efinance>=0.5.5             # Priority 0: EastMoney data source (highest priority) https://github.com/Micro-sheep/efinance
akshare>=1.12.0             # Priority 1: EastMoney web-scraping data source
tushare>=1.4.0              # Priority 2: Tushare Pro API
pytdx>=1.72                 # Priority 2: Tongdaxin quote server
baostock>=0.8.0             # Priority 3: Baostock data
yfinance>=0.2.0             # Priority 4: Yahoo Finance (Fallback)
tickflow>=0.1.0            # TickFlow official SDK (Issue #632, market review enhancement)

# Feishu (Lark)
lark-oapi>=1.0.0             # Feishu (Lark) API SDK

# Data processing
pandas>=2.0.0               # Data analysis
pypinyin>=0.50.0            # Name-to-code resolver (pinyin matching)
openpyxl>=3.1.0             # Excel (.xlsx) parsing for import
numpy>=1.24.0               # Numerical computation
json-repair>=0.55.1         # JSON repair / auto-fix

# AI analysis
# Restore official PyPI install after the quarantined 1.82.7 / 1.82.8 builds were removed.
# Keep the historical minimum version and add a safe upper bound.
litellm>=1.80.10,<1.82.7   # Unified LLM client (Gemini/Anthropic/OpenAI/DeepSeek etc.)
tiktoken>=0.8.0,<0.12.0    # BPE tokenizer for LLM token counting (pin <0.12 to avoid plugin registration issues, #537)
openai>=1.0.0               # OpenAI SDK (transitive dependency of litellm, kept explicit)
PyYAML>=6.0                 # YAML parser for LITELLM_CONFIG support

# Search engines (used to fetch stock news)
tavily-python>=0.3.0        # Tavily Search API (1,000 free requests/month)
google-search-results>=2.4.0  # SerpAPI (100 free requests/month)

# Networking
requests>=2.31.0            # HTTP requests
markdown2>=2.4.0            # Markdown to HTML conversion
imgkit>=1.2.0              # Markdown to image (requires wkhtmltopdf)
fake-useragent>=1.4.0       # Random User-Agent to avoid rate limiting
httpx[socks]                # HTTP client + SOCKS proxy support (optional OpenAI dependency)
dingtalk-stream >= 0.24.3    # DingTalk Stream SDK
# Database
# SQLite is built into Python, no extra install needed

# Discord bot
discord.py>=2.0.0              # Discord bot development library

# Web Content Extraction
newspaper3k>=0.2.8          # Article extraction
lxml_html_clean             # Fix for lxml.html.clean ImportError in newer lxml versions

# Report template engine (Report Engine P0)
jinja2>=3.1.0               # Jinja2 template engine for report rendering

# FastAPI web framework
fastapi>=0.109.0            # Modern Python web framework
uvicorn[standard]>=0.27.0   # ASGI server
python-multipart>=0.0.6     # FastAPI File/Form upload support
